import { colors, spacing, radius, typography, breakpoints, elevation, motion } from '@erp/design-tokens';

export { colors, spacing, radius, typography, breakpoints, elevation, motion };

export const theme = {
  colors,
  spacing,
  radius,
  typography,
  breakpoints,
  elevation,
  motion,
} as const;