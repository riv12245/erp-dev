import { colors, spacing, radius, typography } from '@erp/design-tokens';

export { colors, spacing, radius, typography };

export const theme = {
  colors,
  spacing,
  radius,
  typography,
} as const;